<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude files that contain sensitive information -->
        <!--
        <exclude domain="sharedpref" path="device.xml"/>
        <exclude domain="sharedpref" path="secret_settings.xml"/>
        -->
    </cloud-backup>
    <device-transfer>
        <!-- Exclude files that contain sensitive information -->
        <!--
        <exclude domain="sharedpref" path="device.xml"/>
        <exclude domain="sharedpref" path="secret_settings.xml"/>
        -->
    </device-transfer>
</data-extraction-rules>
